replicaCount: 1

image:
  tag: ""

ingress:
  hosts:
    - host: ${{values.app_name}}-dev.test.com
      paths:
        - path: /
          pathType: Prefix

resources:
  requests:
    cpu: 40m
    memory: 40M
  limits:
    cpu: 200m
    memory: 160M
