replicaCount: 3

image:
  tag: ""

ingress:
  hosts:
    - host: ${{values.app_name}}-prod.test.com
      paths:
        - path: /
          pathType: Prefix

resources:
  requests:
    cpu: 60m
    memory: 60M
  limits:
    cpu: 300m
    memory: 240M
